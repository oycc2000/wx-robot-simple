services:
  wx-robot:
    build: .
    container_name: wx-robot-simple
    ports:
      - "${API_PORT:-3000}:${API_PORT:-3000}"
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env:ro
    restart: unless-stopped
    stdin_open: true
    tty: true
